# 橋の付け根で下層へ潜る床選択の修正（2026-09-11）

報告座標: X 28.366 / Y 14.191 / Z 103.424。

原因: GLBの橋の上り口は、橋面と下層地形が重なっている。歩行先の床選択が「元の高さとの差が小さい面」を優先していたため、歩行可能な高さの上り面が存在しても下層地形を選び続け、橋面の下に潜った。報告地点付近のZ=103.424/108/112の経路で再現し、旧処理では橋面より約1.9〜2.3m低い位置へ進んだ。

修正: chooseReachableGroundで既存の段差上限（WALK_STEP_HEIGHT=.50、既存許容誤差+.02）以下の候補のうち上面を選ぶ。同じ高さなら補間床より実床を優先。橋ごとの座標パッチや床形状の変更は行わず、重なった床の乗り継ぎへ共通適用。

空中の床選択、地形GLB、壁判定、段差上限、重力、継ぎ目補間は維持。すでに橋下にいる場合に頭上の床へ瞬間移動する処理は追加していない。

検証: ローカル版と公開用main版で `python tools/verify_ver5_invariants.py`、`node tools/verify_ver5.cjs --bridges --regression --settings` がPASS。北側・南側の9経路×3機体×30/60fps×往復＝108条件で、橋上の連続移動と終点到達を確認。段差上限、橋下空中から上面に吸着しないこと、橋外で地面へ落下することも確認。既存の192レイキャスト照合、キーボード移動、設定・GT・カメラ・シミュレーション補正の回帰検証も通過。
