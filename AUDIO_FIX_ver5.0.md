# ver5.0 GitHub Pages音声・青燐水補充修正（2026-09-10）

## 原因

GitHub Pagesの音源ファイルは200で配信されているが、`audio/chaser/`のディレクトリ一覧は404。旧実装は一覧取得失敗後、1アクションにつき100版×4拡張子＝400候補を同時にAudioオブジェクトで調べ、各候補を220msで打ち切っていた。通信待ちも時間制限に含まれるため、存在する音源まで「なし」としてキャッシュされる。GT確定音と旧互換パスを持つ補充音は固定パスのため、この探索失敗を受けない。

## 修正

- 生成時に実在するアクション音源の一覧をHTMLへ内包し、版番号の最大値を選択。Pagesのディレクトリ一覧や大量の推測URLに依存しない。同じ版番号なら従来どおりmp3→wav→ogg→m4aを優先する。
- 未登録アクションの補助探索は無印4候補のみ、待機4秒に限定。BGMを音源一覧へ含めない。
- 今後アクション音源のファイル名・版番号を追加／変更した際は、元リポジトリで`python tools/build_ver5.py`を実行し、新しいHTMLを配布する。同名音源の差し替えは一覧更新不要。
- 全機体の青燐水補充は遅延予約を使わず、発動時点でEPを即時回復。CE消費とリキャストは従来どおり。攻撃の命中判定遅延は維持。
- Pagesの公開入口`index.html`と版名付きHTMLを同じ修正版へ更新。RW側にBGMファイルは追加しない。

## 検証

- `python tools/verify_ver5_invariants.py`: 構文・残る基礎仕様照合PASS。
- `node tools/verify_ver5.cjs --audio --settings`: ディレクトリ一覧なし、音声応答450msのHTTPサーバーで全11アクションの実ファイルをブラウザがデコード・再生。選択音源はチェイサー1-2.wav／2-2.wav、ジャスティス1-1.wavを含む最新版。音源要求は11件で、大量探索なし。
- 判定遅延1秒でも全3機体の補充EPが即時+5,000、判定予約の追加なし。攻撃の遅延設定、設定保存・初期化、モード切替、通常描画も検証。
